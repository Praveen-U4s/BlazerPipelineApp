# Azure Pipelines CI for .NET 8 Blazor project
# Triggers on pushes to branch 'master' (also include 'main' for compatibility)
parameters:
- name: environment
  displayName: Target Environment
  type: string
  default: Development
  values:
    - Development
    - Staging
    - Production

trigger:
  branches:
    include:
      - master
      - main

pool:
  vmImage: 'ubuntu-latest'

variables:
- name: buildConfiguration
  value: 'Release'
- name: solution
  value: '**/*.sln'
- name: EnvFileName
  ${{ if eq(parameters.environment, 'Development') }}: value: 'Development'
  ${{ if eq(parameters.environment, 'Staging') }}: value: 'Staging'
  ${{ if eq(parameters.environment, 'Production') }}: value: 'Production'

# Variable Groups
- ${{ if eq(parameters.environment, 'Development') }}:
  - group: Sample-Blazer-App-Dev

- ${{ if eq(parameters.environment, 'Staging') }}:
  - group: Sample-Blazer-App-Staging

- ${{ if eq(parameters.environment, 'Production') }}:
  - group: Sample-Blazer-App-Prod

steps:
- task: NuGetToolInstaller@1
  displayName: 'Install NuGet'

- script: dotnet restore
  displayName: 'dotnet restore'

- script: dotnet build --configuration $(buildConfiguration) --no-restore
  displayName: 'dotnet build'

- script: dotnet publish --configuration $(buildConfiguration) --output $(Build.ArtifactStagingDirectory)/publish /p:EnvironmentName=$(EnvFileName) --no-build
  displayName: 'dotnet publish'
  env:
      ASPNETCORE_ENVIRONMENT: $(EnvFileName)
      ConnectionStrings__DefaultConnection: $(ConnectionStrings.DefaultConnection)

- task: ArchiveFiles@2
  displayName: 'Archive publish output to ZIP'
  inputs:
    rootFolderOrFile: '$(Build.ArtifactStagingDirectory)/publish'
    includeRootFolder: false
    archiveType: 'zip'
    archiveFile: '$(Build.ArtifactStagingDirectory)/publish.zip'
    replaceExistingArchive: true

- task: PublishBuildArtifacts@1
  displayName: 'Publish artifact (ZIP)'
  inputs:
    pathToPublish: '$(Build.ArtifactStagingDirectory)/publish.zip'
    artifactName: 'BlazerPipelineApp'
    publishLocation: 'Container'
